rest:
  template:
    timeout: 40000

voltage:
  auth:
    auth_username: 'Rashmi'

logging:
  level:
    za.co.fnb.pop.branch: debug
  
my:
  greeting: "Hello, I am from config server"
  

management:
  endpoints:
    web:
      exposure:
        include: refresh
        
logging:
  level:
    za.co.fnb.pop.branch: debug
